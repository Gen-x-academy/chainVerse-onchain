# Test Batch Baseline (xeeenab)

Covers: #397, #398, #399, #400

- Escrow refund tests: enforce expiry and token return paths.
- Escrow-vault tests: create, approve release, cancel validations.
- Certificates tests: init once-only behavior, mint/revoke flow checks.
- Add clear pass/fail assertions for auth and state transitions.
