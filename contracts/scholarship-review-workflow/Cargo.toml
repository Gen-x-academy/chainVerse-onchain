[package]
name = "scholarship-review-workflow"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "20.0.4", features = ["contract"] }
shared = { path = "../shared" }
chainverse-types = { path = "../chainverse-types" }
scholarship-core = { path = "../scholarship-core" }
scholarship-programs = { path = "../scholarship-programs" }
scholarship-applications = { path = "../scholarship-applications" }
scholarship-eligibility = { path = "../scholarship-eligibility" }
scholarship-disbursements = { path = "../scholarship-disbursements" }

[dev-dependencies]
soroban-sdk = { version = "20.0.4", features = ["contract", "testutils"] }