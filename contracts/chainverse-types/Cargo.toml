[package]
name = "chainverse-types"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
testutils = ["soroban-sdk/testutils"]

[dependencies]
soroban-sdk = { workspace = true }
